// SPDX-License-Identifier: MIT
pragma solidity ^0.8.10;

library DecimalMath {
    function mul(
        uint256 x,
        uint256 decimalX,
        uint256 y,
        uint256 decimalY
    ) internal pure returns (uint256) {
        uint256 decimalGap = decimalX;
    }
}
